// the ids Ctrl+D and Ctrl+Shift+D reach for, kept apart from the registry so
// the preferences store can read them without pulling in every shader
export const defaultTransitionDuration = 1;
export const defaultVideoTransition = 'cross-dissolve';
export const defaultAudioTransition = 'crossfade';
